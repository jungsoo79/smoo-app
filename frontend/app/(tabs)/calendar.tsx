import { AppScreenPlaceholder } from '@/components/app-screen-placeholder';

export default function CalendarScreen() {
  return (
    <AppScreenPlaceholder
      eyebrow="CALENDAR"
      title="일정"
      description="캘린더와 일정 화면을 이 라우트에서 확장합니다."
    />
  );
}
